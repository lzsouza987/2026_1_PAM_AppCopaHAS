<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppCopaHAS.Views.Jogadores.AlbumView"
             Title="AlbumView">
    <ScrollView>
        <VerticalStackLayout Padding="10,0,0,0"> 
            <CollectionView ItemsSource="{Binding Selecoes}" SelectedItem="{Binding SelecaoSelecionada}" 
                            SelectionMode="Single" HeightRequest="80">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Horizontal" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="5" Padding="10" BackgroundColor="LightBlue">
                            <Label Text="{Binding Pais}" FontAttributes="Bold"/>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            <CollectionView ItemsSource="{Binding Jogadores}">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Horizontal" ItemSpacing="10"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>                        
                        <Border Margin="5" Padding="10" BackgroundColor="LightYellow" WidthRequest="180">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer 
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}},
                                        Path=BindingContext.SelecionarFotoCommand}"
                                    CommandParameter="{Binding .}" />
                            </Border.GestureRecognizers>
                            <VerticalStackLayout>
                                <Image Source="{Binding FotoSource}" HeightRequest="180" WidthRequest="150" Aspect="AspectFill">                                    
                                </Image>
                                
                                <Label Text="{Binding Nome}" FontAttributes="Bold" HorizontalOptions="Center"/>
                                <Label Text="{Binding NumeroCamisa}" HorizontalOptions="Center"/>
                                <Label Text="{Binding Posicao}" HorizontalOptions="Center"/>
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
    
</ContentPage>